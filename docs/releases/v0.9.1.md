# v0.9.1

## UI follow-up

- Add fixed and free-drag account quota layouts while keeping the fixed lower-left layout as the default.
- Show the five-hour quota above the weekly quota and persist free-layout coordinates in Codey Config.
- Display direct model labels such as GPT-5.6 without the redundant official-route prefix.
- Remove the duplicate Appearance settings entry and keep the existing Share-area settings access.
- Unify the Appearance settings surface and preserve background image, overlay, and chat-width controls.
- Keep the native Codex composer anchored at the bottom when a background image is enabled.

## Verification

- GitHub Actions Quality checks passed for Linux and Windows Rust workspaces.
- GitHub Actions Performance audit passed for the UI pull requests.
- Runtime visual behavior remains subject to the post-build Codex page smoke test; this release does not claim a runtime performance improvement.
