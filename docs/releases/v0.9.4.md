# v0.9.4

## 设置界面与模型路由

- 统一额度显示、提示词优化、Codex 外观和子代理设置的标题、开关与说明文案。
- 保留额度固定/自由拖动布局，并修正“自由拖动”文案；背景预览保持 16:9 比例。
- 新增持久化的自动检查更新选项：启动时只检查并提示，不会自动下载或安装。
- 统一模型目录默认显示最多五行，并提供可聚焦、可滚动的右侧滚动区域。
- 修复官方直连路线的模型勾选保存，使统一模型目录真正反映已保存的可用模型。
- 增加带修订号的设置保存恢复逻辑，避免并发更新导致最新设置无法保存。

## 验证

- PR #9 Quality checks 通过：TypeScript、JavaScript、前端构建、Rust 格式化/测试/静态检查，以及 Windows workspace 测试/静态检查。
- PR #9 Performance audit 通过。
- 真实 Codex 页面视觉冒烟测试仍需获得授权的桌面运行时；本版本不宣称已完成该项现场截图验证。
