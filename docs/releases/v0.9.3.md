# v0.9.3

## Settings reliability and UI

- Recover settings saves when a concurrent Codey operation advances Config revision, preserving local edits and unrelated latest changes.
- Retry quota layout persistence after a concurrent revision update without weakening backend stale-write protection.
- Include feature-specific Appearance and quota styles in the embedded Codex settings overlay so the packaged UI matches the redesigned settings view.

## Verification

- GitHub Actions Quality checks passed for Linux TypeScript/JavaScript, frontend production build, Linux Rust, and Windows Rust checks.
- GitHub Actions Performance audit passed.
- Real Codex page visual smoke testing remains subject to an authorized desktop runtime; this release does not claim a completed live visual smoke test.
