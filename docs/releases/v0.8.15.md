# v0.8.15

## Phase 1 UI foundation

- Establish semantic design tokens for Codey's existing light interface.
- Add Mantine-backed shared UI primitives for surfaces, section headers, status chips, field rows, and action groups.
- Migrate the Codex Appearance settings region while retaining background image, opacity, chat width, and existing configuration behavior.
- Keep the existing startup, runtime, routing, injection, sessions, diagnostics, command, and release contracts unchanged.

## Verification

- GitHub Actions Quality checks passed for Linux and Windows Rust workspaces.
- GitHub Actions Performance audit passed with its uploaded source/build/regression artifact.
- This release does not claim a runtime performance improvement; the performance audit remains evidence collection for future comparison.
