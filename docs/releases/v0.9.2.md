# v0.9.2

## UI follow-up

- Keep the fixed lower-left quota layout as the default while allowing the free layout to move across the full Codex background conversation surface.
- Exclude the top conversation toolbar from the free-drag region and keep the existing persisted quota layout coordinates compatible.
- Remove the visible drag label and retain an enlarged six-dot handle with an accessible name.
- Preserve the native Codex composer position, background image behavior, quota ordering, and existing command/config interfaces.

## Verification

- GitHub Actions Quality checks passed for Linux TypeScript/JavaScript, frontend production build, Linux Rust, and Windows Rust checks.
- GitHub Actions Performance audit passed, including the new quota-boundary regression contract.
- Real Codex page visual smoke testing remains subject to an authorized desktop runtime; this release does not claim a completed live visual smoke test.
